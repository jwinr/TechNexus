import styled from "styled-components"

// Page wrap to set width/height for a full page
const FullPageContainer = styled.div`
  margin: 0px auto;
  max-width: 1400px;
  min-height: 100vh;
  background-color: #f3f5f8;
`

export default FullPageContainer
